// const ENV = process.env

const config = {
  language: {
    key: "language",
    initial: "ru",
    list: ["uz", "ru", "en"]
  },
  api: {
    base_url: "https://politex.justns.ru"
  }
};

export default config;
